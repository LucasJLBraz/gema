{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path',''); print(f) if f.endswith('.dart') else None\" | { read -r f; [ -n \"$f\" ] && /opt/flutter/bin/dart format \"$f\"; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Formatting Dart file..."
          }
        ]
      }
    ]
  },
  "enabledPlugins": {}
}
