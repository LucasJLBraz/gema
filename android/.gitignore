gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Exception: shared, low-sensitivity debug keystore committed on purpose so
# every CI build and local `flutter run`/`flutter build apk` sign with the
# same key. Never do this for a real release keystore.
!app/gema-debug.keystore
